# Optional override. The production frontend defaults to the same-origin /api/subscribe route.
VITE_NEWSLETTER_ENDPOINT=/api/subscribe
# Optional override. The production enquiry form defaults to /api/inquiry.
VITE_INQUIRY_ENDPOINT=/api/inquiry
